#
# Windows/macOS lock files contain platform-specific wheel hashes that differ from Linux.
# Regenerate on the target platform with:
#
#   pip-compile --generate-hashes ^
#     --output-file requirements-lock/windows-cpu-py312.txt ^
#     --strip-extras requirements.in
#
# Until regenerated, install without hash verification on Windows:
#   pip install -r requirements-lock/linux-cpu-py312.txt --no-deps --ignore-requires-python
# Or install from requirements.in directly:
#   pip install "numpy>=1.26,<3" "matplotlib>=3.9"
