# データ

本シナリオは **解析解ベースの合成データ (完全にプログラム内生成)** のため、
`data/` ディレクトリに置くファイルはありません。

- コロケーション点: `src/train.py` が実行時に一様乱数でサンプル
- 解析解 (評価用): `analytic_solution(x, t) = exp(-π² α t) sin(π x)` を実行時に計算

外部データセットのダウンロードは不要です。
