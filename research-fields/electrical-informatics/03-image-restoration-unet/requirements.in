# requirements.in — top-level CPU dependencies for E-3 image-restoration U-Net
# Generate locked file:
#   pip install pip-tools
#   pip-compile requirements.in --generate-hashes --output-file requirements.lock
#
# NOTE: torch and torchvision must be installed from the official wheel index FIRST:
#   CPU:  pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cpu
#   CUDA: pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cu124
# Then:
#   pip install -r requirements.lock  (or requirements.txt for range-based install)

scikit-image>=0.24,<1
scipy>=1.13,<2
numpy>=1.26,<3
matplotlib>=3.9
torchmetrics>=1.4,<2
