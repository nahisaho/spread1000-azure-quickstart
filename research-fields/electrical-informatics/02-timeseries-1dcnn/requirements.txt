# CPU-only requirements — regenerate hashes with:
#   pip-compile --generate-hashes requirements.in -o requirements.txt
#
# WORKSHOP MAINTAINER: fill in the correct --hash=sha256:... below after running pip-compile.
# Until then, install with: pip install -r requirements.txt
#
# torch CPU direct wheel (cp312, manylinux_2_28_x86_64):
# torch @ https://download.pytorch.org/whl/cpu/torch-2.7.1%2Bcpu-cp312-cp312-manylinux_2_28_x86_64.whl \
#     --hash=sha256:<FILL_IN>

torch==2.7.1+cpu
numpy>=2.0,<3
scikit-learn>=1.7,<2
matplotlib>=3.9,<4
