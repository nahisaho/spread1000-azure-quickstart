# Generated data and trajectories (regenerated by src/ scripts)
data/*.traj
data/*.extxyz
data/*.cif
data/*.log
data/*.json
data/*.png

# Local AML deploy artifacts
.env

# MACE model cache is intentionally NOT ignored under this repo:
# it lives at ~/.cache/mace/ by default (not inside the repo).

# Python
__pycache__/
*.pyc
.venv/

