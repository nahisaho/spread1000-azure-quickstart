# requirements.txt for MACE-MP-0 NNP quickstart
#
# IMPORTANT: install PyTorch 2.4.0 FIRST, from the official wheel index.
# mace-torch does NOT support PyTorch 2.4.1 or older 2.1.x.
#
#   CPU:  pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cpu
#   CUDA: pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cu121
#
# Then:
#   pip install -r requirements.txt

mace-torch>=0.3.16,<0.4
ase>=3.23,<4
numpy>=1.26,<3
