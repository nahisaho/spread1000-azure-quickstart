# データ

`src/train.py` が実行時に合成生成。事前ダウンロード不要。外部データソースは一切使用していない。

- 真関数: $y(t) = \sin(2\pi t / 5) + 0.1 t$  ($t \in [0, 20]$)
- 観測ノイズ: $\epsilon \sim \mathcal{N}(0, 0.15)$
- 30 点をランダムサンプリング

## ライセンス

このディレクトリ内のデータはプロジェクトが生成した合成データです。外部ソース由来のデータは含まれていません。  
ライセンス: [CC0-1.0](LICENSE) — パブリックドメイン相当。
